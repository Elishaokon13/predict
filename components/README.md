# Components

This directory contains all React components organized by category:

- `ui/` - Reusable UI components (buttons, cards, badges, avatars, etc.)
- `layout/` - Layout components (navigation, headers, footers)
- `features/` - Feature-specific components (trader cards, market cards, charts, etc.)

